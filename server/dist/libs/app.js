// Generated by CoffeeScript 1.10.0
(function() {
  var apiRoutse, app, bodyParser, express, logger, path,userRoutse,taskRoutse,cors;

  express = require('express');

  path = require('path');

  logger = require('morgan');

  bodyParser = require('body-parser');

  apiRoutse = require('../routes/api');
  
  userRoutse = require('../routes/user');
  
  taskRoutse = require('../routes/task');
  cors = require('cors');

  app = express();

  app.use(logger('dev'));
  app.use(bodyParser.json());
  app.use(bodyParser.urlencoded({
    extended: false
  }));

	app.use(cors());
  app.use('/api', apiRoutse);
  app.use('/api', userRoutse);
  app.use('/api', taskRoutse);

  app.use(function(req, res, next) {
    var err;
    err = new Error('Not Found');
    err.status = 404;
    return next(err);
  });

  app.use(function(err, req, res, next) {
    res.send(err.status || 500,{
	  	message: err.message,
	    error: err
	  });
  });

  module.exports = app;

}).call(this);

//# sourceMappingURL=app.js.map
